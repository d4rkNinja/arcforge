interface:
  display_name: "Implement Migrations & Evolution"
  short_description: "Production rules for schema change, backfills, compatibility, and cutover"
  default_prompt: "Use $migration-evolution before changing schemas, running backfills, evolving API/event contracts, synchronizing systems, or cutting over features."
