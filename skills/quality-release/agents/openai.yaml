interface:
  display_name: "Verify Quality & Release Readiness"
  short_description: "Production rules for testing, load, failure drills, and the release checklist"
  default_prompt: "Use $quality-release before claiming a backend change is done: test strategy, concurrency and failure tests, load targets, and the cross-cutting release checklist."
