interface:
  display_name: "Think Through Runtime & Delivery"
  short_description: "Runtime, configuration, shutdown, and delivery decisions"
  default_prompt: "Use $runtime-delivery in Think, Review, Change, or Verify mode for bootstrap, configuration, shutdown, deployment, and CI/CD work."
