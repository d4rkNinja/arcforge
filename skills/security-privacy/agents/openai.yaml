interface:
  display_name: "Implement Security & Privacy"
  short_description: "Production rules for secrets, crypto, TLS, sensitive data, and abuse control"
  default_prompt: "Use $security-privacy before handling secrets, encryption, TLS, sensitive-data classification and deletion, log redaction, abuse protection, or token randomness."
