interface:
  display_name: "Think Through Resilience & Flow Control"
  short_description: "Cache, retry, timeout, quota, and overload decisions"
  default_prompt: "Use $resilience-flow-control in Think, Review, Change, or Verify mode for caching, retries, timeouts, quotas, and overload controls."
