interface:
  display_name: "Implement Transactions & Consistency"
  short_description: "Production rules for isolation, locking, idempotency, and distribution"
  default_prompt: "Use $transactions-consistency before writing transactional or concurrent code: isolation, locking, state machines, idempotency, sagas, sharding, or distributed locks."
