interface:
  display_name: "Think Through Transactions & Consistency"
  short_description: "Transaction, concurrency, and consistency decisions"
  default_prompt: "Use $transactions-consistency in Think, Review, Change, or Verify mode for transactional and concurrent behavior."
