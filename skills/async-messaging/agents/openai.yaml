interface:
  display_name: "Think Through Async Work & Messaging"
  short_description: "Bounded jobs, queues, events, email, and notifications"
  default_prompt: "Use $async-messaging in Think, Review, Change, or Verify mode for jobs, queues, events, outbox or inbox, email, and notifications."
